/* böl ve fethet algoritmasi 3 adımdan oluşur

	breaking the problem into smaller sub-problems
	solving the sub-problems, and
	combining them to get the desired output.

DAC algoritmasında recursive mantığı kullanılır. mesela bir DAC
örneği olan merge sort'ta sürekli yariya bölünür tekli sayilara
ulaşılana kadar, sonra tekli sayilar sıralanir ve finalde de toplanır. */
